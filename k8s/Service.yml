apiVersion: v1
kind: Service
metadata:
  name: library-service
spec:
  selector:
    app: library-app
  ports:
  - port: 80
    targetPort: 8000
  # LoadBalancer on cloud; for minikube/kind use NodePort instead:
  type: NodePort
  # type: LoadBalancer
